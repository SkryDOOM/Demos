<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <FontFamily x:Key="Lato-SemiBold">pack://application,,,,/Style/#Lato Semibold</FontFamily>
    <FontFamily x:Key="Lato-Regular">pack://application,,,,/Style/#Lato Regular</FontFamily>

    <Style TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="FontFamily" Value="{StaticResource Lato-SemiBold}" />
        <Setter Property="FontSize" Value="20"/>
    </Style>

    <Style TargetType="{x:Type Label}" BasedOn="{StaticResource {x:Type Label}}" >
        <Setter Property="FontFamily" Value="{StaticResource Lato-SemiBold}" />
        <Setter Property="FontSize" Value="20"/>
    </Style>

    <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource {x:Type TextBox}}" >
        <Setter Property="FontFamily" Value="{StaticResource Lato-SemiBold}" />
        <Setter Property="FontSize" Value="20"/>
    </Style>

    <Style TargetType="{x:Type ListView}" BasedOn="{StaticResource {x:Type ListView}}" >
        <Setter Property="FontFamily" Value="{StaticResource Lato-SemiBold}" />
        <Setter Property="FontSize" Value="20"/>
    </Style>
    
</ResourceDictionary>
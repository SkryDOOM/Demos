namespace EventAssistant
{
    /// <summary>
    /// Public enum that contains the available pages.
    /// </summary>
    public enum PagesEnum : byte
    {
        TODAY = 0,
        HOME = 1,
        UPCOMING = 2,
        HISTORY = 3,
        COMPLETED = 4,
    }
}
